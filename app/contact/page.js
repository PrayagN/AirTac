import React from "react";
import ContactForm from "./ContactForm";

export const metadata = {
  title: "Contact & Support",
  description: "Get in touch with PlayOnMeet support. Submit feature feedback, report game issues, or inquire about partnerships.",
};

export default function ContactPage() {
  return <ContactForm />;
}
